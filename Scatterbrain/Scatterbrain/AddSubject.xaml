<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:inputLayout="clr-namespace:Syncfusion.XForms.TextInputLayout;assembly=Syncfusion.Core.XForms"
             xmlns:models="clr-namespace:Scatterbrain.Models"
             x:Class="Scatterbrain.AddSubject"
             x:Name="root">
    <ContentPage.Content>
        <StackLayout VerticalOptions="StartAndExpand" HorizontalOptions="FillAndExpand">
            <StackLayout.BindingContext>
                <models:Subject/>
            </StackLayout.BindingContext>
            <Label Text="Department"/>
            <Entry Text="{Binding Department}" Placeholder="Grocery"/>
            <Label Text="Subject"/>
            <Entry Text="{Binding Description}" Placeholder="Garlic"/>
            <Button Text="Remember" Command="{Binding Source={x:Reference root}, Path=BindingContext.Add}" CommandParameter="{Binding}" Clicked="Close"/>
            <Button Text="Nevermind" Clicked="Close"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>