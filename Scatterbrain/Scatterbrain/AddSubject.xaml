<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:inputLayout="clr-namespace:Syncfusion.XForms.TextInputLayout;assembly=Syncfusion.Core.XForms"
             xmlns:models="clr-namespace:Scatterbrain.Models"
             x:Class="Scatterbrain.AddSubject"
             x:Name="root">
    <ContentPage.Content>
        <StackLayout VerticalOptions="StartAndExpand" HorizontalOptions="FillAndExpand">
            <StackLayout.BindingContext>
                <models:Subject/>
            </StackLayout.BindingContext>
            <Label Text="Отдел"/>
            <Entry Text="{Binding Department}" Placeholder="Бакалея"/>
            <Label Text="Товар"/>
            <Entry Text="{Binding Description}" Placeholder="Чеснок"/>
            <Button Text="Запомнить" Command="{Binding Source={x:Reference root}, Path=BindingContext.Add}" CommandParameter="{Binding}" Clicked="Close"/>
            <Button Text="Неважно" Clicked="Close"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>